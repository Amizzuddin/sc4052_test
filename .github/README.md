# CI/CD Pipeline — generated by cicd-gen

This pipeline was automatically generated and committed to the
`cicd-gen/setup/python/documentation3` branch.  Review the file before merging into your default branch.

## Generated file

| Platform | Config file |
|---|---|
| `github-actions` | `.github/workflows/ci.yml` |

## Changing which branches trigger CI

Open `.github/workflows/ci.yml` and find the `on:` block near the top:

```yaml
on:
  push:
    # Runs on every branch. To restrict, replace '**' with a list:
    # branches: [main, develop, 'release/**']
    branches: ["**"]
  pull_request:
    branches: ["**"]
```

Replace `["**"]` with the branch names you want, for example:

```yaml
    branches: [main, develop]
```

## Making changes

1. Review the generated config in `.github/workflows/ci.yml`.
2. Edit it directly on GitHub or check out the `cicd-gen/setup/python/documentation3` branch locally.
3. Open a Pull Request from `cicd-gen/setup/python/documentation3` into your default branch when satisfied.

## Resources

- [GitHub Actions docs](https://docs.github.com/en/actions)
- [GitLab CI/CD docs](https://docs.gitlab.com/ee/ci/)
- [Jenkins Pipeline docs](https://www.jenkins.io/doc/book/pipeline/)

---
*Generated by [cicd-gen](https://github.com) — AI-powered CI/CD generator.*
